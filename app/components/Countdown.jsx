import React, {
    PropTypes,
} from 'react';

const Countdown = React.createClass({
    render() {
        return (
            <div>Countdown.jsx</div>
        );
    }
});

export default Countdown;
